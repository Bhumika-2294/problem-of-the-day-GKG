class Solution
{
    public:
    //Function to count nodes of a linked list.
    int getCount(struct Node* head){
    
    int count=1;
    Node*curr=head;
    while(curr->next!=nullptr)
    {
        count++;
        curr=curr->next;
    }
    return count;
    }
};
    
